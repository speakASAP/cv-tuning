apiVersion: v1
kind: Service
metadata:
  name: cv-tuning-frontend
  namespace: statex-apps
  labels:
    app: cv-tuning-frontend
spec:
  type: ClusterIP
  selector:
    app: cv-tuning-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
